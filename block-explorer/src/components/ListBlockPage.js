import React, {PropTypes} from 'react';

const ListBlockPage = ({blocks}) => {
   <div className="col-md-6">
      // blocks;

      testing

   </div>

}

ListBlockPage.propTypes = {
  blocks: PropTypes.array
}

export default ListBlockPage

